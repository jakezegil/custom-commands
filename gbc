#!/bin/bash

if [[ "$1" == "-h" || "$1" == "--help" ]]; then
    if [[ -f "$(dirname "$0")/gbc.help" ]]; then
        echo -e "$(cat "$(dirname "$0")/gbc.help")"
    else
        echo "Help file not found"
    fi
    exit 0
fi

git branch --show-current
